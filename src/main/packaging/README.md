Place platform icons here:

- `src/main/packaging/app-icon.icns` for macOS packages
- `src/main/packaging/app-icon.ico` for Windows packages
- `src/main/packaging/app-icon.png` as fallback

The `jpackageApp` Gradle task auto-selects the best icon for the current OS.
